version: '3.8'

services:
  backend:
    image: quocsy2/mern-backend:1.0
    container_name: mern-backend-hub
    ports:
      - "5000:5000"
    dns:
      - 8.8.8.8
      - 8.8.4.4
    environment:
      MONGODB_URI: "mongodb+srv://s234232_db_user:Iu4S3kUjFkjtVfRB@234232.vishx8b.mongodb.net/cloud_lab?retryWrites=true&w=majority"
      PORT: 5000
    restart: always

  frontend:
    image: quocsy2/mern-frontend:2.0
    container_name: mern-frontend-hub
    ports:
      - "3000:3000"
    depends_on:
      - backend
    restart: always